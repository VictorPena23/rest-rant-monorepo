{
  "dependencies": {
    "api": "^6.0.0",
    "express": "^4.18.2",
    "i": "^0.3.7",
    "nodemon": "^2.0.22"
  },
  "scripts": {
    "start": "nodemon index",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
